{
  "name": "Storyteller",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && yarn start",
    "server": "nodemon server.js",
    "update-all": "yarn && cd client && yarn",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "express": "^4.16.2",
    "nodemon": "^1.18.3"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
